[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["backend"]
addopts = "--ignore=tests/ai_assistant"
markers = [
    "ai: AI assistant tests requiring live FMAPI (deselected by default)",
    "databricks: tests requiring live Databricks/Lakebase access (set LAKEMETER_RUN_DATABRICKS_TESTS=1 to run)",
]

[tool.coverage.run]
source = ["backend/app"]

[tool.coverage.paths]
source = [
    "backend/app",
    "app",
]

[tool.coverage.report]
show_missing = true
exclude_lines = [
    "pragma: no cover",
    "if __name__ == .__main__.",
]
